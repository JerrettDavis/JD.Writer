{
  "name": "jd-writer-studio-lite-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright tests for JD.Writer Studio Lite (GitHub Pages endpoint).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
